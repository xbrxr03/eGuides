# Raw source material (PDFs, carousel screenshots, reel/video captures,
# transcripts) — kept locally only, never published.
**/source/

# OS junk
.DS_Store
Thumbs.db
._*

# Playwright MCP scratch snapshots from screenshot capture
.playwright-mcp/
